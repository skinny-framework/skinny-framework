-@val s: skinny.Skinny

%h3 #{s.i18n.get("skill.new")}
%hr

-for (e <- s.errorMessages)
 %p(class="alert alert-danger") #{e}

%form(method="post" action={uri("/skills")} class="form")
 =include("_form.html.scaml")
